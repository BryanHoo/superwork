# {{ package_name }}/{{ layer }} Index

## Scope

Rules and contracts for the `{{ package_name }}` package in the `{{ layer }}` layer.

## Pre-Development Checklist

- Read `.superwork/spec/guides/index.md`
- Read `./{{ layer_guideline }}`
- Confirm whether this task is feature work or bug work before implementation

## Verification Checklist

- Run the most relevant test command for this package
- Run `{{ verification_command }}` if no narrower command exists
- Review changed files against `./{{ layer_guideline }}`

## Update Triggers

- New `{{ layer }}` rule or contract
- New validation or error behavior
- New test requirement for this package/layer
