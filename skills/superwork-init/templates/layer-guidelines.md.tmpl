# {{ package_name }} {{ layer_title }} Guidelines

## Purpose

Capture durable rules for `{{ package_name }}` in the `{{ layer }}` layer.

## Rules

- Keep changes scoped to the current task.
- Prefer existing patterns in the same package before inventing new ones.

## Contracts

- Document new request/response, config, state, or data contracts here.

## Good Examples

- Add project-local examples when a stable pattern appears.

## Bad Examples

- Record repeated mistakes that future sessions should avoid.

## Test Notes

- List the tests or assertions that prove this layer behaves correctly.

## Update Notes

- Update this file when a new durable rule or pitfall appears.
